FROM golang:1.25-alpine AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /bin/appointment-agent .

FROM alpine:3.20
WORKDIR /app
COPY --from=build /bin/appointment-agent /usr/local/bin/appointment-agent
CMD ["appointment-agent"]
